import { pageMetadata } from '@/lib/site';
export const metadata = pageMetadata('About', 'Meet Shakthi Yoga and learn about our mindful approach to movement and wellbeing.', '/about');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
