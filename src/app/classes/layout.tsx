import { pageMetadata } from '@/lib/site';
export const metadata = pageMetadata('Yoga Classes', 'Browse Shakthi Yoga classes by schedule, instructor, and experience level.', '/classes');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
