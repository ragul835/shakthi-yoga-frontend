import { pageMetadata } from '@/lib/site';
export const metadata = pageMetadata('Contact', 'Contact Shakthi Yoga with questions about classes, passes, and your practice.', '/contact');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
