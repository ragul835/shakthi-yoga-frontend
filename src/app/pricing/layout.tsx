import { pageMetadata } from '@/lib/site';
export const metadata = pageMetadata('Class Passes', 'Explore flexible Shakthi Yoga class-pass options, manually verified by our team.', '/pricing');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
