import { pageMetadata } from '@/lib/site';
export const metadata = pageMetadata('Privacy Policy', 'How Shakthi Yoga collects, uses, protects, and retains personal information.', '/privacy');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
