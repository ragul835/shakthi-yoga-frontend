import { pageMetadata } from '@/lib/site';
export const metadata = pageMetadata('Terms of Service', 'Terms governing Shakthi Yoga accounts, class requests, passes, and participation.', '/terms');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
