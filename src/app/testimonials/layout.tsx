import { pageMetadata } from '@/lib/site';
export const metadata = pageMetadata('Testimonials', 'Read experiences shared by members of the Shakthi Yoga community.', '/testimonials');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
